import { Size } from "@/styles";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  scroll: { marginHorizontal: -Size[6] },
  scrollContent: { paddingHorizontal: Size[6] },
});

export default styles;
