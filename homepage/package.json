{
  "private": true,
  "name": "@auralous/homepage",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-preact": "^1.1.3",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "marked": "^3.0.7",
    "preact": "^10.1.0",
    "preact-markup": "^2.1.1",
    "preact-render-to-string": "^5.1.2",
    "preact-router": "^3.2.1"
  }
}
